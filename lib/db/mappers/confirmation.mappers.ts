import "server-only"

// Auto-generated DTO mapper stubs.
// These should convert Prisma records into transport-safe DTO/read models.

export function mapConfirmationParticipantSummary(record: unknown): unknown {
  return record
}

export function mapAggregateConfirmationStatus(record: unknown): unknown {
  return record
}

export function mapConfirmPresenceEligibility(record: unknown): unknown {
  return record
}
