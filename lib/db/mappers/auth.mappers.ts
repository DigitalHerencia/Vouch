import "server-only"

// Auto-generated DTO mapper stubs.
// These should convert Prisma records into transport-safe DTO/read models.

export function mapSessionUser(record: unknown): unknown {
  return record
}

export function mapAuthzSnapshot(record: unknown): unknown {
  return record
}

export function mapContextualParticipantRole(record: unknown): unknown {
  return record
}
