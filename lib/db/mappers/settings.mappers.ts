import "server-only"

// Auto-generated DTO mapper stubs.
// These should convert Prisma records into transport-safe DTO/read models.

export function mapAccountSettings(record: unknown): unknown {
  return record
}

export function mapProfileBasics(record: unknown): unknown {
  return record
}

export function mapTermsStatusCard(record: unknown): unknown {
  return record
}
