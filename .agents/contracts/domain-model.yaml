schema_version: "2.0.0"
contract_name: "domain-model"
product: "Vouch"
source_of_truth_date: "2026-04-30"

summary:
  description: "Canonical domain model for Vouch after the merchant/customer pricing migration."
  core_sentence: "Vouch is a commitment-backed payment coordination system for pre-arranged appointments and in-person agreements."
  stripe_account:
    source_account_id: "acct_1TQHH2GuFcEUvSe9"
    display_name: "Vouch"

global_invariants:
  - id: "no_product_marketplace"
    description: "Vouch must not provide discovery, listings, public profiles, matching, booking, search, categories, recommendations, reviews, ratings, or messaging."
  - id: "no_arbitration"
    description: "Vouch must not decide disputes, collect evidence, adjudicate claims, or let admins award funds based on human judgment."
  - id: "no_escrow_claim"
    description: "Vouch must not describe itself as escrow or claim direct custody of funds."
  - id: "provider_backed_money"
    description: "Payment state is represented by provider-backed references and statuses, not raw card data, identity documents, full provider payloads, or local custody."
  - id: "merchant_customer_model"
    description: "The provider/merchant creates the Vouch; the customer/client accepts and pays; the provider/merchant is the payout recipient."
  - id: "server_authoritative"
    description: "Identity, authz, payment state, Vouch state, pricing, confirmation eligibility, and lifecycle resolution are server-authoritative."

legacy_mapping:
  status: "active_migration"
  rule: "Prefer merchant/provider/customer/client language in product UI and new code. Existing payer/payee identifiers may remain temporarily where destructive renames are unsafe."
  mappings:
    payer: "provider / merchant / creator"
    payee: "customer / client / paying acceptor"
    amountCents: "legacy alias for protectedAmountCents"
    platformFeeCents: "legacy alias for vouchServiceFeeCents during migration"

canonical_terms:
  preferred:
    - "merchant"
    - "provider"
    - "customer"
    - "client"
    - "commitment-backed payment"
    - "payment coordination"
    - "appointment"
    - "in-person agreement"
    - "confirm presence"
    - "release funds"
    - "refund"
  prohibited_or_restricted:
    - term: "escrow"
      rule: "Do not use without legal approval."
    - term: "marketplace"
      rule: "Do not describe the product as a marketplace; Stripe Connect/platform taxonomy is infrastructure-only."
    - term: "dispute"
      rule: "Do not create dispute-resolution product flows."
    - term: "review"
      rule: "Do not add ratings, reviews, reputation, or social proof surfaces."

money_model:
  currency: "usd"
  protectedAmountCents:
    description: "Amount the merchant/provider receives when the Vouch releases."
  merchantReceivesCents:
    formula: "protectedAmountCents"
  vouchServiceFeeCents:
    formula: "max(ceil(protectedAmountCents * 0.05), 500)"
  processingFeeOffsetCents:
    formula: "ceil((protectedAmountCents + vouchServiceFeeCents + stripeFixedCents) / (1 - stripePercentBps / 10000) - (protectedAmountCents + vouchServiceFeeCents))"
    defaults:
      stripePercentBps: 290
      stripeFixedCents: 30
  customerTotalCents:
    formula: "protectedAmountCents + processingFeeOffsetCents + vouchServiceFeeCents"
  applicationFeeAmountCents:
    formula: "vouchServiceFeeCents + processingFeeOffsetCents"
  stripe_payment_intent:
    amount: "customerTotalCents"
    application_fee_amount: "applicationFeeAmountCents"
    transfer_data_destination: "merchant/provider connected account"

enums:
  vouch_status: ["pending", "active", "completed", "expired", "refunded", "canceled", "failed"]
  participant_role:
    legacy_values: ["payer", "payee"]
    preferred_meaning:
      payer: "provider / merchant"
      payee: "customer / client"
  payment_status:
    - "not_started"
    - "requires_payment_method"
    - "authorized"
    - "captured"
    - "release_pending"
    - "released"
    - "refund_pending"
    - "refunded"
    - "voided"
    - "failed"

entities:
  User:
    description: "Authenticated account holder."
  ConnectedAccount:
    description: "Stripe connected account for the merchant/provider payout recipient."
  PaymentCustomer:
    description: "Stripe customer/payment-method reference for the customer/client who pays."
  Vouch:
    description: "Merchant-created commitment-backed payment agreement accepted and paid by a customer."
    required_money_fields:
      - "protectedAmountCents"
      - "merchantReceivesCents"
      - "vouchServiceFeeCents"
      - "processingFeeOffsetCents"
      - "applicationFeeAmountCents"
      - "customerTotalCents"
    legacy_money_fields:
      - "amountCents"
      - "platformFeeCents"
    constraints:
      - "Provider/merchant creator is immutable after creation."
      - "Customer/client acceptor is null until acceptance."
      - "Provider/merchant must not accept their own Vouch."
      - "Pricing must be calculated server-side."
      - "Provider/merchant receives protectedAmountCents on successful release."
  PaymentRecord:
    description: "Provider-backed payment state and pricing snapshot for a Vouch."
    required_money_fields:
      - "protectedAmountCents"
      - "merchantReceivesCents"
      - "vouchServiceFeeCents"
      - "processingFeeOffsetCents"
      - "applicationFeeAmountCents"
      - "customerTotalCents"
  RefundRecord:
    description: "Provider-backed refund, void, or non-capture outcome."
  PresenceConfirmation:
    description: "Participant confirmation of presence. Existing legacy roles map to provider/customer during migration."

forbidden_entities:
  - "PublicProfile"
  - "Review"
  - "Rating"
  - "MessageThread"
  - "ServiceListing"
  - "MarketplaceCategory"
  - "Recommendation"
  - "DisputeCase"
  - "EvidenceUpload"
  - "ReputationScore"
