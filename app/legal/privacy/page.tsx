import { LegalPage } from "@/features/marketing"

export default function PrivacyRoute() {
  return (
    <LegalPage
      title="Privacy"
      description="Vouch minimizes collected data and stores only provider references, readiness flags, timestamps, participant state, and audit-safe metadata required for deterministic payment coordination."
    />
  )
}
