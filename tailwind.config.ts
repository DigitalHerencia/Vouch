import type { Config } from "tailwindcss"

// Tailwind CSS v4 is configured from CSS by default.
// This file stays intentionally minimal for tools that still look for a config path.
const config: Config = {
  plugins: [],
}

export default config
